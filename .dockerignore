# Ignore files not needed when building a Docker image of this project

# Ignore environment file since there are could be some sensitive credentials
.env

# Ignore the temporary files:
/tmp/*
/log/*.log

# Ignore the gems installed by bundler, as they should be installed from zero
# by the Docker image building process:
/vendor/bundle
.bundle

# Ignore shell & console artifacts, such as bash, byebug and pry history logs:
.bash_history
.byebug_hist
.pry_history

# Ignore the docker-compose project files:
docker-compose.yml

# To avoid including the project's dependency cache in Docker's build context
.semaphore-cache
