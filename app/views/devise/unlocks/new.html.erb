<%  title 'Resend unlock instructions' %>

<%= simple_form_for(resource, :as => resource_name, :url => unlock_path(resource_name)) do |form| %>
  <%= form.inputs do %>
    <%= form.input :email, :required => true %>
  <% end %>

  <%= form.button :submit, 'Resend unlock instructions' %>
<% end %>
