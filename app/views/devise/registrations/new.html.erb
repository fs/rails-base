<% title %Q{Sign up to "#{configatron.app_name}"} %>

Already member? <%= link_to 'Sign in here!', new_session_path(resource_name) %>
<br /><br />

<%= semantic_form_for(resource, :validate => true, :as => resource_name, :url => registration_path(resource_name)) do |form| %>
  <%= form.inputs do %>
    <%= form.input :full_name, :required => true %>
    <%= form.input :email, :required => true %>
    <% if resource.password_required? %>
      <%= form.input :password, :required => true %>
      <%= form.input :password_confirmation, :required => true %>
    <% end %>
  <% end %>

  <%= form.buttons do %>
    <%= form.commit_button 'Sign up' %>
  <% end %>
<% end %>

<%= render :partial => 'devise/shared/oauth' %>
